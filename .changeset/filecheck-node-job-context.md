---
'filecheck-node': patch
---

Type `context` (JobContext — artworkSize, pageCount, fileCount, bleed, safety) on CreateJobParams, PreflightParams, and FixParams. The API already accepted it; the types now do too.
