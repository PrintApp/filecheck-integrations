---
'filecheck-mcp': patch
---

list_catalog surfaces profile kinds: the text summary breaks entries down by kind (e.g. "11 profiles (7 pdf, 4 raster)") and the tool description tells agents to pick a profile whose kind matches the file's type.
